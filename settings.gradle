rootProject.name = 'gradle-script-kotlin'

include 'provider',
        'tooling-models',
        'tooling-builders',
        'compiler-plugin',
        'plugins',
        'test-fixtures',
        'samples-tests'
