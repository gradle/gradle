include 'gradle-wrapper'
include 'gradle-core'
include 'gradle-ui'
include 'gradle-open-api'

rootProject.name = 'gradle'
rootProject.children.each {project ->
    project.projectDir = new File(settingsDir, "subprojects/$project.name")
}
