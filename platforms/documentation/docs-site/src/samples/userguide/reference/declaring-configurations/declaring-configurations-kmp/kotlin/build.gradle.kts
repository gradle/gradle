// Multi-module KMP + Android application project.
// The KMP source set configurations demonstrated in the docs live in `shared/build.gradle.kts`.
// The launchable Android application lives in `app/build.gradle.kts` and depends on `:shared`.
