plugins {
    id 'application'
}

def myBuildGroup = "My app build"

// tag::app-grouping[]
tasks.build {
    group = myBuildGroup
}

tasks.check {
    group = myBuildGroup
    description = 'Runs checks (including tests).'
}

tasks.named('run') {
    group = myBuildGroup
}
// end::app-grouping[]
