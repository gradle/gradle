---
/**
 * Explanation list for AsciiDoc-style code callouts. Wraps a plain markdown
 * ordered list; the numbers render as bubbles matching the ones the
 * gradle-callouts Expressive Code plugin draws in the preceding code block.
 *
 * <CalloutList>
 *
 * 1. The class declaration.
 * 2. The method.
 *
 * </CalloutList>
 */
---

<div class="gd-callout-list"><slot /></div>
