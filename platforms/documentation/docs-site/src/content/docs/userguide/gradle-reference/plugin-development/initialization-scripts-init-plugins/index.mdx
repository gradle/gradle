---
title: "Initialization Scripts and Init Plugins"
---

Initialization scripts (_init scripts_) are Gradle scripts that run during the **initialization phase**, **before** the build’s `settings.gradle(.kts)` and project build scripts are evaluated.

Init scripts are useful for setting up common configuration such as enterprise repositories, plugin resolution rules, build listeners, or applying custom plugins across many builds without modifying each repository.

:::caution
Init scripts affect **every** build they are attached to (local or CI), so treat them like production code.
:::

## Using an init script

Init scripts are Groovy/Kotlin scripts whose receiver is the [`Gradle`]({groovyDslPath}/org.gradle.api.invocation.Gradle.html) object.

They can:

- Configure enterprise-wide defaults (e.g., `pluginManagement`, mirrors)
- Vary configuration by environment (dev vs. CI)
- Provide user-specific data (e.g., credentials via providers)
- Define machine-specific details (e.g., JDK/SDK locations)
- Register build listeners and custom loggers

One important [limitation](sharing_build_logic_between_subprojects.mdx#sec:using_buildsrc) is that init scripts cannot access classes from `buildSrc` in the target build.

## Invoking an init script

Init scripts are discovered and executed in this order:

1. Command line: `-I` / `--init-script path/to/init.gradle(.kts)` (can be repeated)
2. `$[GRADLE_USER_HOME](directory_layout.mdx#dir:gradle_user_home)/init.gradle(.kts)`
3. Any `\*.init.gradle(.kts)` in `$[GRADLE_USER_HOME](directory_layout.mdx#dir:gradle_user_home)/init.d/` (alphabetical)
4. Any `\*.init.gradle(.kts)` in `$[GRADLE_HOME](installation.mdx#sec:linux_macos_users_2)/init.d/` (alphabetical)

All found scripts run; scripts in the same directory run alphabetically.

## Writing an init script

Like any Gradle script, an init script implements [`Script`]({groovyDslPath}/org.gradle.api.Script.html) and delegates to the [`Gradle`]({groovyDslPath}/org.gradle.api.invocation.Gradle.html) instance.

:::note
Properties from `gradle.properties` are available on `Settings`/`Project`, **not** on `Gradle`.
:::

Useful `Gradle` callbacks include: `settingsEvaluated {}`, `projectsLoaded {}`, `beforeProject {}`, `afterProject {}`.
You can see more in the [Gradle Lifecycle API](build_lifecycle.mdx#lifecycle_api).

### Configuring projects from an init script

You can use an init script to configure the projects in the build.
This works similarly to configuring projects in a multi-project build.

The following sample shows how to perform extra configuration from an init script _before_ the projects are evaluated:

{/* converter-gap: node=sample dir="snippets/reference/other-topics/configurationInjection/groovy" files="build.gradle[];init.gradle[]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/configurationInjection/kotlin" files="build.gradle.kts[];init.gradle.kts[]" */}

This sample uses this feature to configure an additional repository to be used only for specific environments.

```bash
$ ./gradlew --init-script init.gradle.kts -q showRepos
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/configurationInjection/tests-common/initScriptConfiguration.out[]
```

```bash
$ ./gradlew --init-script init.gradle -q showRepos
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/configurationInjection/tests-common/initScriptConfiguration.out[]
```

## Adding external dependencies

Use `initscript { }` to declare the init script’s classpath (repositories + dependencies):

{/* converter-gap: node=sample dir="snippets/reference/other-topics/externalDependency/kotlin" files="init.gradle.kts[tags=declare-classpath]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/externalDependency/groovy" files="init.gradle[tags=declare-classpath]" */}

The closure passed to `initscript` configures a [`ScriptHandler`]({javadocPath}/org/gradle/api/initialization/dsl/ScriptHandler.html) instance.
You declare the init script classpath by adding dependencies to the `classpath` configuration.

This is the same way you declare, for example, the Java compilation classpath.
You can use any of the dependency types described in [Declaring Dependencies](declaring_dependencies.mdx#one-declaring-dependencies), except project dependencies.

Having declared the init script classpath, you can use the classes in your init script as you would any other classes on the classpath.
The following example adds to the previous example and uses classes from the init script classpath:

{/* converter-gap: node=sample dir="snippets/reference/other-topics/externalDependency/kotlin" files="init.gradle.kts[tags=all];build.gradle.kts[]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/externalDependency/groovy" files="init.gradle[tags=all];build.gradle[]" */}

```bash
$ ./gradlew --init-script init.gradle.kts -q doNothing
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/externalDependency/tests-common/externalInitDependency.out[]
```

```bash
$ ./gradlew --init-script init.gradle -q doNothing
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/externalDependency/tests-common/externalInitDependency.out[]
```

## Init plugins

An init plugin is a Gradle plugin whose target type is `Plugin&lt;Gradle&gt;` and is applied in an init script.

Unlike project (`Plugin&lt;Project&gt;`) or settings (`Plugin&lt;Settings&gt;`) plugins, an init plugin attaches to the Gradle object during the initialization phase, before the settings and projects are configured:

- **Scope**: Runs at init; can access `settingsEvaluated`, `projectsLoaded`, `beforeProject`, `afterProject` [callbacks](build_lifecycle.mdx#lifecycle_api) to safely interact with settings/projects when they exist.
- ***Distribution***: Applied from an init script (`~/.gradle/init.d/**.init.gradle(.kts)` or `$GRADLE_HOME/init.d`) so it affects every build on that machine/agent. However, this does not include **all*** builds: for example, builds run via `TestKit` use an isolated [Gradle user home](directory_layout.mdx#dir:gradle_user_home), and different users or build agents may each have their own init scripts.

The init plugin in the init script ensures that only a specified repository is used when running the build:

{/* converter-gap: node=sample dir="snippets/reference/other-topics/plugins/kotlin" files="init.gradle.kts[tags=init-script-plugin];build.gradle.kts[tag=show-repos-task]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/plugins/groovy" files="init.gradle[tags=init-script-plugin];build.gradle[tag=show-repos-task]" */}

```bash
$ ./gradlew --init-script init.gradle.kts -q showRepositories
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/plugins/tests-common/usePluginsInInitScripts.out[]
```

```bash
$ ./gradlew --init-script init.gradle -q showRepositories
```

```text
Unresolved directive in init_scripts.adoc - include::{snippetsPath}/reference/other-topics/plugins/tests-common/usePluginsInInitScripts.out[]
```

## Applying plugins from an init script

You can apply plugins in an init script just like in `build.gradle(.kts)`/`settings.gradle(.kts)`.

When applying plugins within the init script, Gradle instantiates the plugin and calls the plugin instance&#8217;s [`Plugin.apply(T)`]({javadocPath}/org/gradle/api/Plugin.html#apply-T-) method.

The `gradle` object is passed as a parameter, which can be used to configure all aspects of a build.
Of course, the applied plugin can be resolved as an external dependency as described in [External dependencies for the init script](#sec:custom_classpath).

In this example, the init script is its own project called `baseline-init-plugin`:

{/* converter-gap: node=sample dir="snippets/reference/other-topics/init-kotlin/kotlin" files="baseline-init-plugin/src/main/kotlin/com/example/BaselineInitPlugin.kt[]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/init-groovy/groovy" files="baseline-init-plugin/src/main/groovy/com/example/BaselineInitPlugin.groovy[]" */}

You can publish it to a local Maven repository by running:

```bash
$ ./gradlew :baseline-init-plugin:publishPluginMavenPublicationToMavenRepoInLocalBuildRepository
```

Here it is applied in an init script of the `app` project:

{/* converter-gap: node=sample dir="snippets/reference/other-topics/init-kotlin/kotlin" files="init.gradle.kts[]" */}

{/* converter-gap: node=sample dir="snippets/reference/other-topics/init-groovy/groovy" files="init.gradle[]" */}

Then you can run the app with:

```bash
$ ./gradlew :app:build -I init.gradle(.kts)
```