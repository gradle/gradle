plugins {
    `java-library`
}

group = "com.squareup.okhttp"
version = "2.7.5"

// Minimal stub implementation if you just need it to compile locally
// You must at least provide an empty class or minimal code
