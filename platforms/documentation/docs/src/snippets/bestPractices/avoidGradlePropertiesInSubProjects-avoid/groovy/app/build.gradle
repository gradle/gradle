// This file is located in /app
tasks.register("printProperties") { // <1>
    def propA = project.getProperty("propertyA") // <2>
    def propB = project.getProperty("propertyB")

    doLast {
        println "propertyA in app: $propA"
        println "propertyB in app: $propB"
    }
}
