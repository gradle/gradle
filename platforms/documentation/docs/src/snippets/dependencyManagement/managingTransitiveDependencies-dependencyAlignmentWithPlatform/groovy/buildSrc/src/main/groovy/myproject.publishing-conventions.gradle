plugins {
    id 'maven-publish'
}

group = rootProject.group
version = rootProject.version

publishing {
    repositories {
        maven {
            url = uri(layout.buildDirectory.dir\("repo"\))
        }
    }
}
