plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        serverPlugin {
            id = 'org.myorg.server'
            implementationClass = 'org.myorg.ServerPlugin'
        }
    }
}
