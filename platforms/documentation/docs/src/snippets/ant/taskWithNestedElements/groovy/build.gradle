tasks.register('zip') {
    doLast {
        ant.zip(destfile: 'archive.zip') {
            fileset(dir: '../common/src') {
                include(name: '**.xml')
                exclude(name: '**.java')
            }
        }
    }
}
