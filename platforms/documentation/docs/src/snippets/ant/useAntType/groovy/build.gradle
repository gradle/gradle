tasks.register('list') {
    doLast {
        def path = ant.path {
            fileset(dir: '../common/libs', includes: '*.jar')
        }
        path.list().each {
            println it
        }
    }
}
