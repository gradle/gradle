plugins {
    id("gradlebuild.distribution.implementation-java")
}

description = "Integration of :dependency-management and :resources. Responsible for downloading and caching external artifacts."

dependencies {
    api(projects.buildOperations)
    api(projects.credentialsApi)
    api(projects.daemonServices)
    api(projects.fileTemp)
    api(projects.hashing)
    api(projects.logging)
    api(projects.persistentCache)
    api(projects.resources)
    api(projects.serialization)
    api(projects.startParameter)
    api(projects.stdlibJavaExtensions)

    api(libs.jspecify)
    api(libs.slf4jApi)

    implementation(projects.baseServices)
    implementation(projects.credentials)
    implementation(projects.enterpriseOperations)

    implementation(libs.commonsIo)
    implementation(libs.commonsLang)
    implementation(libs.guava)
    implementation(libs.jsr305)

    testImplementation(testFixtures(projects.core))

    testFixturesImplementation(projects.unitTestFixtures)
}

gradleModule {
    computedRuntimes {
        // Auto-generated by `:checkTargetRuntimes --fix`
        daemon = true
    }
}
