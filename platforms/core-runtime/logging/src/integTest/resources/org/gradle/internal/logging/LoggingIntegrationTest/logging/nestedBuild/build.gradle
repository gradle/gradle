package org.gradle.internal.logging.LoggingIntegrationTest.logging.nestedBuild

println 'nestedBuild quiet'
logger.info 'nestedBuild info'

task log {
    doLast {
        println 'nestedBuild task quiet'
    }
}
