apply plugin: "groovy"

repositories {
    mavenCentral()
}

compileGroovy {
    if (JavaVersion.current().isJava9()) {
        options.compilerArgs.addAll(['--add-modules', 'javafx.graphics'])
        groovyOptions.forkOptions.jvmArgs.addAll(['--add-modules', 'javafx.graphics'])
    }
}
