usePlugin('groovy')

sourceCompatibility=1.5
targetCompatibility=1.5

dependencies { 
    addMavenRepo()

	compile "org.codehaus.groovy:groovy-all:1.5.6"

	clientModule(['testCompile'],'org.testng:testng:5.8:jdk14') {
	    artifact {
	        name = 'testng'
	        type = 'jar'
	        extension = 'jar'
	        confs = ['jdk14']
        }
	}
}

test {
   useTestNG().options.dumpCommand().javadocAnnotations() 
}
