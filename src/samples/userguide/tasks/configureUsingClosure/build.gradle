// START SNIPPET declare-task
task myCopy(type: Copy)
// END SNIPPET declare-task

myCopy {
   from(file('resources'))
   into(file('target'))
   includes('**/*.txt', '**/*.xml', '**/*.properties')
}
