usePlugin 'java'

task show << {
    source.main.classesDir = new File(buildDir, 'output')
    println relativePath(processResources.destinationDir)
    println relativePath(compileJava.destinationDir)
    println relativePath(convention.source.main.classesDir)
}