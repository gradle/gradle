usePlugin('java')

task check << {
    source.main.classesDir = new File(buildDir, 'output')
    println(processResources.destinationDir.name)
    println(compile.destinationDir.name)
    println(convention.source.main.classesDir.name)
}