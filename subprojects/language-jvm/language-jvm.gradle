dependencies {
    compile libraries.groovy
    compile project(":languageBase")
    compile project(":core")
}

// TODO:DAZ Fix the cycle and switch this back on (cycle imported from 'plugins' subproject)
//useClassycle()
useTestFixtures()
