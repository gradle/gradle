import org.gradle.performance.plugin.MeasurementPlugin

buildscript {
    dependencies {
        classpath files("${relativePath(new File(gradleTask.project.rootProject.findProject(":performance").buildDir, 'libs/measurement-plugin.jar')).replace('\\\\','/')}")
    }
}

apply plugin: MeasurementPlugin

${original}

