apply plugin: "java"

compileJava {
  options.fork = true
  options.forkOptions.useAntForking = false
  source += files("src/main/java/compile/fork/resource.txt")
}


dependencies {
    compile localGroovy()
}