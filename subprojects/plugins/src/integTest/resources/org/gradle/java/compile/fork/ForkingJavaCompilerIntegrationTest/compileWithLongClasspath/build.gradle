apply plugin: "java"

compileJava.options.fork = true
compileJava.options.forkOptions.useAntForking = false

dependencies {
    compile localGroovy()
    compile files((1..999).collect { "${dependencyCacheDir}/library${it}.jar" })
}
