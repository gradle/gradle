apply plugin: "java"

compileJava.options.with {
    useAnt = false
    fork = true
    forkOptions.with {
        memoryInitialSize = '64m'
        memoryMaximumSize = '128m'
    }
}

dependencies {
    compile localGroovy()
}