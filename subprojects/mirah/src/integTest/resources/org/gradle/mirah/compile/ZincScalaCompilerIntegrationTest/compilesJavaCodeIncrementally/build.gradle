apply plugin: "mirah"

repositories {
    mavenCentral()
}

dependencies {
    compile "org.mirah-lang:mirah-library:$mirahVersion"
}

tasks.withType(MirahCompile) {
    mirahCompileOptions.with {
        useAnt = false
        fork = true
    }
}

