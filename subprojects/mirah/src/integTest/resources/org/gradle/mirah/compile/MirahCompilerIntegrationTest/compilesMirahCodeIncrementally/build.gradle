apply plugin: "mirah"

repositories {
    mavenCentral()
}

dependencies {
//  compile "org.mirah:mirah-library:$mirahVersion"
}

tasks.withType(MirahCompile) {
    mirahCompileOptions.with {
        fork = true
    }
}

