subprojects {
    apply plugin: "mirah"

    repositories {
        mavenCentral()
    }

    dependencies {
//      compile "org.mirah:mirah-library:$mirahVersion"
    }

    tasks.withType(MirahCompile) {
        mirahCompileOptions.with {
            useAnt = false
            fork = true
        }
    }
}

project(":prj2") {
    dependencies {
        compile project(":prj1")
    }
}

