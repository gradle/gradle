apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.13'
}

buildDir = 'target'
sourceSets.main.java.outputDir = new File(buildDir, 'main-classes')
sourceSets.test.java.outputDir = new File(buildDir, 'test-classes')
