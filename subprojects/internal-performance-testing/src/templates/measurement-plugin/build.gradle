import org.gradle.performance.plugin.BuildEventTimeStamps
import org.gradle.performance.plugin.MeasurementPlugin

buildscript {
    dependencies {
        classpath files("${relativePath(new File(gradleTask.project.rootProject.findProject(":internalPerformanceTesting").buildDir, 'libs/measurement-plugin.jar')).replace('\\\\','/')}")
    }
}

apply plugin: MeasurementPlugin

${original}

BuildEventTimeStamps.settingsEvaluated()

