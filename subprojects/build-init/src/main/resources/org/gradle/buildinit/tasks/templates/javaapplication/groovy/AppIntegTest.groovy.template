/*
 * This Spock specification was generated by the Gradle 'init' task.
 */
${packageDecl.statement}
import java.net.HttpURLConnection
import spock.lang.Specification

class AppIntegTest extends Specification {
    def "gradle website is reachable"() {
        when:
        def url = "https://www.gradle.org".toURL()
        def con = url.openConnection()

        then:
        con.getResponseCode() == 200
    }
}
