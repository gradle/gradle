dependencies {
    integTestCompile project(':testKit')
}

processIntegTestResources {
    from ("$rootDir/gradle") {
        into "org/gradle/smoketests/cache-init-scripts"
        include "overlappingTaskOutputsStatsInit.gradle"
        include "taskCacheDetailedStatsInit.gradle"
    }
}

useTestFixtures()
