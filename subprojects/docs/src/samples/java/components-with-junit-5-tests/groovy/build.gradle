allprojects {
    version = '1.0.2'
    group = 'org.gradle.sample'

    repositories {
        jcenter()
    }
}

subprojects {
    apply plugin: "java"

    test {
        useJUnitPlatform()
    }
}
