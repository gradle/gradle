commands: [{
    executable: gradle
    args: build -x lint -x lintVitalRelease
    execution-subdirectory: android-app
    # Do not fail for deprecation warning in Kotlin Android plugin
    flags: "--warning-mode=all"
}]
