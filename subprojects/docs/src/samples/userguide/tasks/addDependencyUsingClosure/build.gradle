throw new Exception("log level: $project.logging.level std out capture level: $project.logging.standardOutputCaptureLevel std error capture level: $project.logging.standardErrorCaptureLevel")

task taskX << {
    println 'taskX'
}

taskX.dependsOn {
    tasks.findAll { task -> task.name.startsWith('lib') }
}

task lib1 << {
    println 'lib1'
}

task lib2 << {
    println 'lib2'
}

task notALib << {
    println 'notALib'
}
