plugins {
    id('java-gradle-plugin')
}

repositories {
    jcenter()
}

gradlePlugin {
    plugins {
        download {
            id = 'org.gradle.sample.download'
            implementationClass = 'DownloadPlugin'
        }
    }
}
