plugins {
    id 'product'
}

// A custom build element. This defines the product to build
product {
    displayName 'Basic Edition'
    // Which modules to include in this product?
    module project(':identity-management')
    module project(':billing')
}
