apply plugin: "java"
apply plugin: "sonar"

group = "com.sonar.test"
version = "1.0"
description = "Sonar Test Project"

sonar {
    server {
        url serverUrl
    }
    database {
        url databaseUrl
        driverClassName = "org.h2.Driver"
    }
}

