rootProject.name = "gradle-kotlin-dsl"

include(
    "provider",
    "tooling-models",
    "tooling-builders",
    "plugins",
    "plugins-experiments",
    "test-fixtures",
    "samples-tests",
    "integ-tests")
