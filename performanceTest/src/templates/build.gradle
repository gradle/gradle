<% if (subprojects.empty ) { %>
usePlugin 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.5'
}

test {
    options.forkOptions.forkMode = ForkMode.ONCE
    testReport = false
}
<% } %>