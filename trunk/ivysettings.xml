<ivysettings>
    <property name="ibiblio-maven2-root"
              value="http://repo1.maven.org/maven2/" override="false"/>
    <property name="maven2.pattern"
              value="[organisation]/[module]/[revision]/[module]-[revision]"/>
    <property name="maven2.pattern.ext"
              value="${maven2.pattern}.[ext]"/>

    <!-- pull in the local repository -->
    <include url="${ivy.default.conf.dir}/ivyconf-local.xml"/>

    <settings defaultResolver="default" checkUpToDate="false"/>
    <resolvers>
        <ibiblio name="maven2"
                 root="${ibiblio-maven2-root}"
                 pattern="${maven2.pattern.ext}"
                 m2compatible="true"
                 validate="false"
                />
        <dual name="gradle" allownomd="true">
            <url name="sf_ivy">
                <ivy pattern="http://gradle.sourceforge.net/repository/[organisation]/[module]/ivy-[revision].xml"/>
            </url>
            <url name="jar_maven2" m2compatible="true">
                <artifact pattern="http://gradle.sourceforge.net/repository/${maven2.pattern.ext}"/>
                <artifact pattern="${ibiblio-maven2-root}/${maven2.pattern.ext}"/>
            </url>
        </dual>
        <chain name="default">
            <resolver ref="local"/>
            <resolver ref="gradle"/>
            <resolver ref="maven2"/>
        </chain>
    </resolvers>
</ivysettings>