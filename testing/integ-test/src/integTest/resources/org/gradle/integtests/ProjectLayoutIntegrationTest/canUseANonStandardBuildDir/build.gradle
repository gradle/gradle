plugins {
    id("java")
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.13'
}

project.layout.buildDirectory.set(project.layout.projectDirectory.dir("target"))
sourceSets.main.java.destinationDirectory.set(project.layout.buildDirectory.dir('main-classes'))
sourceSets.test.java.destinationDirectory.set(project.layout.buildDirectory.dir('test-classes'))
