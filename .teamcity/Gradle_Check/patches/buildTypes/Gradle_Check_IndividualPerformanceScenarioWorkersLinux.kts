package Gradle_Check.patches.buildTypes

import jetbrains.buildServer.configs.kotlin.v2019_2.*
import jetbrains.buildServer.configs.kotlin.v2019_2.ui.*

/*
This patch script was generated by TeamCity on settings change in UI.
To apply the patch, change the buildType with uuid = 'Gradle_Check_IndividualPerformanceScenarioWorkersLinux' (id = 'Gradle_Check_IndividualPerformanceScenarioWorkersLinux')
accordingly, and delete the patch script.
*/
changeBuildType(uuid("Gradle_Check_IndividualPerformanceScenarioWorkersLinux")) {
    check(paused == false) {
        "Unexpected paused: '$paused'"
    }
    paused = true
}
