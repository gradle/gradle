tasks.withType(Compile).all { task ->
    task.options.encoding = 'utf-8'
}
tasks.withType(GroovyCompile).all { task ->
    task.options.encoding = 'utf-8'
    task.groovyOptions.encoding = 'utf-8'
}
